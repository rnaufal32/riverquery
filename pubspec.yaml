name: riverquery
description: "TanStack Query-inspired data fetching & caching for Flutter, built on Riverpod."
version: 0.1.0
homepage: https://github.com/rnaufal32/riverquery

environment:
  sdk: ^3.7.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0
  riverpod: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
